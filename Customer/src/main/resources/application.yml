server:
  port: 8200
spring:
  application:
    name: customer
  datasource:
    password: root
    url: jdbc:postgresql://localhost:5432/grpc_customer
    username: postgres
  jpa:
    hibernate:
      ddl-auto: create-drop
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
    show-sql: true
